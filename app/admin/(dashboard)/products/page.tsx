import { ProductsManager } from "@/app/components/admin/managers/products-manager";

export const metadata = {
  title: "Products | Anita Printers Admin",
};

export default function ProductsPage() {
  return <ProductsManager />;
}
